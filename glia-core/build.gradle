publishing {
    publications {
        gliaCore(MavenPublication) {
            groupId = 'com.dhenry.glia'
            artifactId = 'glia-core'
            version = project.publishedVersion

            from components.java
            artifact kotlinSourcesJar
            updatePom(pom)
        }
    }
}