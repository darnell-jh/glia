version '1.0.0'

publishing {
    publications {
        gliaCore(MavenPublication) {
            groupId = 'com.dhenry.glia'
            artifactId = 'glia-core'
            version = '1.0.0'

            from components.java
        }
    }
}