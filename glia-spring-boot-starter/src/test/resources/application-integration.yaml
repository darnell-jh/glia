spring:
  application:
    name: testapp
  rabbitmq:
    username: guest
    password: guest
    template:
      exchange: eventbus
  data:
    cassandra:
      cluster-name: testcluster
      keyspace-name: ${spring.application.name}
glia:
  rabbit:
    event-packages:
    - "com.dhenry.domain.events.consumed"